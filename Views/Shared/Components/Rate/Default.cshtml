@model RateVM

@for (int i = 0; i < 5; i++)
{
    if (i < Model.Rate)
    {
        <span id="st@(i + 1)_@Model.PostId" class="fa fa-star checked"></span>
    }
    else
    {
        <span id="st@(i + 1)_@Model.PostId" class="fa fa-star"></span>
    }
}
